# 编码规范

> 本文档由 init skill 自动生成。标注 `[自动检测]` 的内容已从项目配置推断，需确认。
> 标注 `[手动填写]` 的内容需要人工补充。

---

## 通用规则

<!-- 所有语言/框架都应遵守的通用编码约束 -->

- 禁止使用 `any` 类型，用 `unknown` 或具体类型替代（[自动检测] / [手动填写]）
- 多个独立数据源的并行请求使用 `Promise.allSettled`，不使用 `Promise.all`（[自动检测] / [手动填写]）
- [其他通用规则]

---

## 后端规范

### [表现层 / Controller / Handler]

- 参数校验使用 [校验方式，如 Pydantic / class-validator / ...] — [自动检测]
- 统一异常处理：[异常处理方式] — [手动填写]
- 返回值格式：[统一响应格式] — [手动填写]

### [业务层 / Service]

- 事务边界在 Service 层管理
- 复杂业务逻辑拆分为私有方法，单个方法不超过 [N] 行 — [手动填写]
- [其他约定]

### [数据层 / DAO / Repository]

- [ORM/查询方式] — [自动检测]
- [SQL 写法约定] — [手动填写]
- [其他约定]

### 后端测试规范

- 测试目录：[tests/ / src/__tests__/ / ...] — [自动检测]
- 命名：`test_[被测函数]_[场景]_[预期结果]`
- Mock 策略：[什么 mock、什么不 mock] — [手动填写]
- 每个 [接口/函数] 至少覆盖：正常路径 + 边界条件 + 异常路径
- 数据构造：[fixture 路径 / 工厂方法 / 硬编码] — [手动填写]

---

## 前端规范

### 组件库约束

- 使用 [组件库名称，如 shadcn-vue / Ant Design Vue / ...] — [自动检测]
- 禁止哪些原生 HTML 元素：
  | 原生元素 | 替代组件 | 备注 |
  |---------|---------|------|
  | `<button>` | `<Button>` | 所有按钮 |
  | `<input>` | `<Input>` | 所有输入 |
  | `<select>` | `<Select>` | 所有下拉 |
  | `<table>` | `<Table>` | 所有表格 |
  | `<dialog>` | `<Dialog>` | 所有弹窗 |

### 样式系统

- CSS 方案：[Tailwind CSS / CSS Modules / styled-components / ...] — [自动检测]
- Token 使用规则：引用 `docs/design-system.md` 中的设计 Token
- 禁止硬编码颜色值（如 `#333`, `rgb(...)`），必须用 CSS 变量
- 禁止 magic spacing（如 `p-[17px]`），用标准 Tailwind scale
- 亮/暗模式：通过 CSS 变量自动切换，不需要手动处理

### 状态管理

- [Pinia / composable + ref / 其他] — [自动检测]
- 使用边界：
  - 只在一个组件内使用 → 本地 ref
  - 需要跨组件共享但没有跨页面 → composable
  - 需要跨页面共享 / 需要持久化 → store

### 错误处理

- 字段级错误 → FormMessage（在字段下方显示）
- 表单级错误 → toast.error()
- 全局错误 → toast.error()（网络异常、401、服务端错误）

### 代码结构限制

- `<template>` ≤ 400 行
- `<script setup>` ≤ 300 行
- [其他限制]

### 前端测试规范

- 测试框架：[Vitest / Jest / ...] — [自动检测]
- 组件测试模式：[描述]
- 测试目录：[路径] — [自动检测]

---

## 禁止事项清单

> 每条配反面例子和正面例子。

1. **禁止 [具体禁止事项]**
   - ❌ 错误：`[反面代码]`
   - ✅ 正确：`[正面代码]`

2. **禁止 [具体禁止事项]**
   - ❌ 错误：`[反面代码]`
   - ✅ 正确：`[正面代码]`

<!-- 按需添加，每条都来自真实踩坑 -->
