# CLAUDE.md 模板

> 使用方式：复制到项目根目录，按提示填空。控制在 100-120 行。
> 编码规范详见 `docs/standards.md`，不再内嵌在 CLAUDE.md 中。
>
> 本模板覆盖 xyz-harness 全部 skill 期望从项目读取的上下文。标注 **[必需]** 的章节如果不填，harness 对应阶段会跳过或降级。

---

# CLAUDE.md

## 项目背景 **[必需]**

<!-- 一句话说清项目是什么 -->
本项目是 [项目名称]，用于 [解决什么问题]。

### 技术栈

- 语言：[Python 3.12 / TypeScript 5.x / Rust 1.7x / Java 1.8 / ...]
- 框架：[FastAPI / Next.js / Actix-web / Spring Boot / ...]
- 关键依赖：[列出 3-5 个核心依赖及其用途]
- 数据库：[PostgreSQL / MySQL / SQLite / ...]
- 缓存：[Redis / Memcached / 无]
- 消息队列：[Kafka / RabbitMQ / 无]

### 模块结构

```
[项目根]/
├── [模块A]/       # [模块A的职责]
├── [模块B]/       # [模块B的职责]
└── tests/         # 测试
```

## 文档索引 **[必需]**

> harness 所有 agent 通过此索引定位项目文档。标准文档出现在索引中则必须存在。

| 文档 | 路径 | 类型 | 说明 |
|------|------|------|------|
| 编码规范 | `docs/standards.md` | 标准 | 前后端编码规范、测试规范、命名约定 |
| 系统架构 | `docs/architecture.md` | 标准 | 技术栈、领域模型、存储、API 结构（纯前端项目可删除） |
| 前端设计系统 | `docs/design-system.md` | 标准 | 组件库、token、样式约束、参考组件（纯后端项目可删除） |

<!-- 可选：L3 自由文档（wiki/、docs/adr/ 等），类型标注"自由" -->

## 架构约束 **[必需]**

> 这些是不可违反的硬规则。每条规则对应一个历史踩坑案例。

### 分层规则

- [表现层/Controller] 只做参数校验和异常处理，不写业务逻辑
- [应用层/Service] 编排业务逻辑，不直接操作数据库
- [数据层/DAO/Repository] 只做数据存取，不包含业务判断
- 禁止跨层调用：[具体禁止规则]

### 外部调用规范

- 所有外部 HTTP 调用必须设置超时（默认 [X] 秒）
- 所有外部调用必须有降级/fallback 方案

### 禁止事项清单

1. **禁止 [具体禁止事项]**
   - ❌ 错误：`[反面代码]`
   - ✅ 正确：`[正面代码]`

<!-- 按需添加，每条都来自真实踩坑 -->

## 质量门禁 **[必需]**

> 所有条件必须可程序化验证。gate-script.sh 会解析此章节中的命令行。

### 编译/类型检查

- 编译: `[具体命令]`

### 测试

- 测试: `[具体命令]`

### Lint

- lint: `[具体命令]`

## 部署 **[可选]**

- 部署命令: `[具体命令]`
- 目标环境: [staging / production]
- 健康检查: `[URL]`
- 验证接口: `[1-2 个核心接口 URL]`

## 已知陷阱

> 每次 Agent 犯错后在这里补一条。按时间倒序排列。

### [yyyy-MM-dd] [问题描述]
- **现象：** [Agent 做错了什么]
- **根因：** [为什么会出错]
- **规则：** [新增/修改了什么规则来防止]

## Harness Agent 覆盖 **[可选]**

> 项目可以覆盖 harness 默认的 agent 配置。
